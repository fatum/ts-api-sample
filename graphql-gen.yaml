overwrite: true
schema: "./graphql/schema.graphql"
documents: null
generates:
  ./graphql/types.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql/debug.json:
    plugins:
      - "introspection"
